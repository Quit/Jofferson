<Window x:Class="Jofferson.ErrorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Name}" Height="400" Width="800"
        Icon="App.ico">
    <DataGrid ItemsSource="{Binding Errors}" AutoGenerateColumns="false">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Resource" Binding="{Binding Resource}" Width="Auto"/>
            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
        </DataGrid.Columns>
    </DataGrid>
</Window>
